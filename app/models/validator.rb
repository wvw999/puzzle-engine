require_relative 'board'

class Validator

# needs to iterate over all words on board, in all possible combinations
# attempting to remove letters if they are neighbors && the next letter in the word
# will need some kind of function like: if current letter has two neighbor letters which could be the next letter, remember to try them both
# build out set of all possible moves?



end
